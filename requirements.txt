# This file lists the dependencies required for the project.

# Boto3 is the Amazon Web Services (AWS) SDK for Python, which allows Python developers to write software that makes use of Amazon services like S3 and EC2.
boto3 == 1.26.137

# python-dotenv reads key-value pairs from a .env file and can set them as environment variables.
python-dotenv==1.0.0

# Requests is a simple, yet elegant, HTTP library for Python.
requests==2.32.0

# self is a library for creating self-contained Python applications.
self~=2020.12.3

# Botocore is a low-level interface to a growing number of Amazon Web Services.
botocore~=1.29.165